Create table tb_Cliente 
	(CPF varchar(11), 
	Nome varchar(100),
    Endereco1 varchar (150),
    Endereco2 varchar (150),
    Bairo varchar (50),
    Cidade varchar(50), 
    Estado varchar(50),
    CEP varchar(8), 
    Idade smallint, 
    Sexo varchar(1), 
    `Limite Credito` float,
    Volume_Compra float,
    is_primeira_compra bit(1))